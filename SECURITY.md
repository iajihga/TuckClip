# 安全政策

## 报告漏洞

请优先使用项目托管平台 Security 页面中的私密漏洞报告入口。如果尚未启用，只创建一条不含漏洞细节的 issue，请求维护者提供私下联系渠道。

不要在公开 issue、讨论区或 pull request 中发布利用代码、真实剪贴板内容、密钥、个人路径、DPAPI 数据样本或其他敏感信息。

报告中建议包含：

- 受影响的 commit 或版本
- 操作系统完整版本与处理器架构
- 安装来源，以及 macOS 签名/公证或 Windows Authenticode/SmartScreen 状态
- 可重复的最小步骤、预期行为、实际影响和安全降级是否发生
- 已去除个人数据的日志或演示材料
- 你认为合理的缓解方式

维护资源有限，修复会优先落在当前开发分支。是否回补旧版本、何时公开细节，会根据影响和修复准备情况单独说明，不预设固定响应时限。

## 数据与信任边界

TuckClip 处理的历史可能包含密码、令牌、文件路径、图片和私人文本。应用自身是 local-only：没有账号、遥测、远程 API、云同步或更新检查，不主动上传剪贴板内容。

这不意味着内容永远不会离开设备。当前系统剪贴板、Apple 通用剪贴板、Windows 云剪贴板、系统备份、目标应用、杀毒软件和第三方同步工具都有独立的数据规则，不属于 TuckClip 的控制边界。

### macOS

- 历史位于当前用户的 `~/Library/Application Support/TuckClip/`，没有额外应用层加密。
- 私密、瞬时、自动生成 pasteboard 标记、排除 bundle identifier、密码管理器和私钥检测均为尽力过滤。
- 自动粘贴依赖辅助功能/事件投递权限。无法确认权限、目标、条目或剪贴板所有权时必须停止投递并降级为只复制。
- 项目当前不启用 App Sandbox。扩大文件访问、进程交互或网络能力需要单独安全审查。

### Windows

- 数据位于当前用户的 `%LOCALAPPDATA%\TuckClip`。
- 历史元数据和图片使用 Windows DPAPI `CurrentUser` 范围保护；普通偏好设置保存为不含剪贴板正文的 JSON。
- DPAPI 是本机静态保护，不是端到端加密。与当前用户同权限运行的恶意进程可能调用 DPAPI、读取当前剪贴板或窃取解密后的内容；管理员和系统级攻击者也不在此防护模型之外。
- DPAPI 数据与 Windows 用户配置文件绑定。换用户、丢失配置文件或某些系统迁移后可能无法恢复；解密或结构校验失败时应用应保留原文件并进入只读/错误状态。
- 已有 `settings-v1.json` 损坏、无法读取或校验失败时，原文件保持不变，应用以暂停记录、关闭自动粘贴和图片捕获的安全值启动；不得在加载失败时 fail-open 到开启记录。
- TuckClip 尊重 `ExcludeClipboardContentFromMonitorProcessing`、`CanIncludeInClipboardHistory=0` 和 `CanUploadToCloudClipboard=0` 等标记，并支持按进程排除。来源应用未正确声明时仍可能遗漏。
- 捕获时的剪贴板 sequence 与 owner 必须在完整提取前后保持稳定；变化中的快照必须整体丢弃，不能把一个进程的内容按另一进程归因后持久化。
- 自动粘贴以普通用户权限恢复目标、写入并核对剪贴板序列，再用 `SendInput` 投递 `Ctrl+V`。任何核对失败都必须只复制。
- Windows UIPI 会阻止普通权限 TuckClip 向管理员目标注入输入。这是安全边界，不是应通过提升 TuckClip、`uiAccess=true` 或关闭系统保护绕过的问题。清单固定为 `asInvoker`、`uiAccess=false`。

文件历史只保存原路径，不复制原文件；路径本身也可能泄露用户名、项目名或目录结构。

## 发布资产边界

当前自动化发布状态：

- macOS DMG 使用 ad-hoc 签名，没有 Developer ID 身份或 Apple 公证。
- Windows portable ZIP 和 Inno Setup 安装器没有 Authenticode 签名，可能触发 Microsoft Defender SmartScreen。
- 每个版本提供统一 SHA-256 文件；校验值只能证明下载文件与发布者列出的字节一致，不能替代源码审阅、身份签名或恶意软件分析。

Gatekeeper 或 SmartScreen 拦截未签名构建本身不是漏洞。以下情况属于安全问题：发布说明错误声称已签名/公证、校验值不匹配、同 tag 资产被替换，或工作流把错误架构包装成另一个文件名。

不要建议使用者全局关闭 Gatekeeper、SmartScreen、杀毒软件或 UAC。安装器保持当前用户范围，不请求管理员权限。

## 值得报告的问题

- 排除应用或隐私标记可被稳定绕过，导致高风险内容进入历史
- DPAPI purpose、用户范围、明文临时文件或错误处理使 Windows 历史意外裸露
- 路径遍历、符号链接、reparse point 或文件名处理造成越权读写/删除
- 损坏历史被静默覆盖，或保存失败后仍删除有效图片/元数据
- 自动粘贴发送到错误进程、错误窗口、错误条目，或在安全检查失败后仍发送输入
- TuckClip 被意外提权，或存在绕过 UIPI 的危险配置
- 恶意图片、文本或文件列表造成持久崩溃、超量资源消耗或代码执行
- 暂停状态失效，界面显示暂停但后台仍持续记录
- 发布资产、签名、公证、架构或 SHA-256 被替换或错误声明

单纯发现过滤规则无法识别任意未知密码管理器，通常属于加固建议；如果可以用常见应用和稳定步骤造成高风险收集，请按安全问题私下报告。

## 测试要求

只在你拥有或获准使用的设备、账户和数据上测试。避免读取他人的剪贴板历史、干扰日常输入、向管理员窗口反复注入事件或公开尚未修复的利用方式。

优先使用最小合成夹具。不要为了测试私钥过滤而复制真实私钥，不要附带真实 DPAPI blob、用户目录、密码管理器内容或企业文件名。
